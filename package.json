{
  "name": "dsmeventos-web-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:mocks": "node ./mocks/mock-servers.js",
    "start:gateway": "cd ./api-gateway && npm install --no-audit --no-fund && npm run dev",
    "dev": "concurrently \"npm run start:mocks\" \"npm run start:gateway\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
